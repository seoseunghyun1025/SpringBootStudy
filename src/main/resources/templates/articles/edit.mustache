{{>layouts/header}}
<form class="container" action="/articles/update" method="post">
    <input type="hidden" name="articleUserid" value="{{article.user.id}}">
    <!-- 작성자 아이디 -->
    <input type="hidden" name="id" value="{{article.id}}"><!-- id를 보내야하기 때문에 hidden으로 작성 후 보냄 -->
    <input type="text" class="form-control" name="title" value="{{article.title}}">
    <textarea class="form-control" rows="3" name="content">{{article.content}}</textarea>
    <button type="submit" class="btn btn-primary"> 확인</button>
    <a href="/articles/{{article.id}}">Back</a>
</form>
{{>layouts/footer}}